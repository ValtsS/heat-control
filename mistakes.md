# mistakes.md – what we got wrong (and how to avoid)

Append-only. Each entry is a verified pitfall from this repo, not generic advice.

| Date       | Area               | Mistake                                                                                                                         | Correct Approach                                                                                                    | Ref                                                    |
| ---------- | ------------------ | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| 2026-08-21 | branch             | Worked on `master` (`isSunUp=hours>=8`) but `dumb` is target (`elevation`+`midday`)                                             | `git checkout dumb` first; `git branch -a` lists `origin/dumb` vs `origin/master`                                   | `src/control.ts:2-173`, `src/sun.ts:1`                 |
| 2026-08-21 | toolchain          | Ran global `npx tsc`/`npx eslint` → fails (`tsc` shim, `eslint@10` legacy)                                                      | `npm ci` then `./node_modules/.bin/tsc --noEmit`, `./node_modules/.bin/eslint` (`8.47.0`)                           | `package.json:11`, `Dockerfile:1`                      |
| 2026-08-21 | control/state      | Tests shared global `currentState` → flaky order-dependent                                                                      | Exported `resetControlStateForTest:115`/`getControlStateForTest:118`; call in `beforeEach`                          | `src/control.ts:110-118`                               |
| 2026-08-21 | control/time       | Used `nowNs + 5s` then `nowNs + 16s` with mutated `nowNs` → 3 s drift, `TurningOff` flipped to `Off` early                      | Use absolute `base + ms` offsets (`base=1e12`) not accumulation                                                     | `src/control.getState.test.ts:105-139`                 |
| 2026-08-21 | control/transition | Expected `TurningOff` to stay after hold even with `enable=true`                                                                | Transition is **unconditional**: `TurningOn→On`, `TurningOff→Off` next non-gated call (`src/control.ts:193-198`)    | `src/control.ts:193`                                   |
| 2026-08-21 | solar mock         | Mocked `Date.prototype.getUTCHours` – insufficient for `sun.ts` (uses full `N`, `EoT`, `H`)                                     | `jest.spyOn(sun,'getSunElevationUTC')`/`minutesFromSolarMiddayUTC` or replace `global.Date` entirely                | `src/sun.ts:7-33`, `src/sun.test.ts:7`                 |
| 2026-08-21 | sun calc           | Assumed `isSunUp` / `suncalc` lib used                                                                                          | `isSunUp` only on `master`; `dumb` computes declination `23.44*sin(...)` + `EoT` inline, ignores `suncalc` dep      | `src/sun.ts:1-81`                                      |
| 2026-08-21 | DefaultSettings    | Copied master curve `50→0,52→1250…` into dumb tests → failures                                                                  | Dumb curve is `-5→-2000,48→0,50→200,53→1000,55→2400,57→3550` (`src/control.ts:40-67`)                               | `src/control.calculateRequiredPower.test.ts:1`         |
| 2026-08-21 | sun test           | `minutesFromSolarMidday` at 10:20 expected `<15` but EoT adds ~16 min → flaky                                                   | Allow `<25` or compute EoT-aware expectation                                                                        | `src/sun.test.ts:44`                                   |
| 2026-08-21 | hysteresis         | Checked `power>required` without `heaterOn` offset                                                                              | `power > required - (heaterOn?2496:0)` and **avail** for solar bypass is `power+(heaterOn?2496:0)`                  | `src/control.ts:4,167,173`                             |
| 2026-08-21 | NaN                | `GetState(NaN)` assumed disabled via guard – actually `calculateRequiredPower(NaN)`→`NaN`→`false` but no explicit `isNaN` check | Test expects `false`/`TurningOff`; add explicit guard if needed at `index.ts:42`                                    | `src/index.ts:42`, `src/control.ts:166`                |
| 2026-08-21 | console            | Left `console.log` mocked incorrectly → noisy logs in CI                                                                        | `jest.spyOn(console,'log').mockImplementation(()=>{})` in `beforeEach`                                              | `src/control.ts:175`                                   |
| 2026-08-21 | forecast/rate      | Polled Solcast every hour → hit 10/day limit (`429`)                                                                            | Default `FORECAST_INTERVAL_MS=21600000` (6h) for Solcast, 1h for Open-Meteo via `scheduler.ts:7`                    | `src/forecast/scheduler.ts:7`, `solcastProvider.ts:17` |
| 2026-08-21 | forecast/provider  | Single `global_tilted_irradiance` call for mixed E+S – needs 2 fetches summed `irr*kWp*eff/1000`                                | `openMeteoProvider.ts:22` `Promise.all(arrays.map(fetchArray))` merged, tested with 2 `fetch` mocks                 | `src/forecast/openMeteoProvider.test.ts:11`            |
| 2026-08-21 | forecast/cache     | `SqliteForecastStore.close()` sync then `unlink` → `EBUSY` / `SQLITE_MISUSE: Database handle is closed`                         | `close():Promise` must be awaited before unlink; use `:memory:` for empty case                                      | `src/forecast/cache.ts:94`, `cache.test.ts:14`         |
| 2026-08-21 | control/defer      | `shouldDeferMorning` checked `T>=40` → `T=48` never deferred, 05Z test failed (`100>0` still true)                              | Raise to `T>=50` and require `power>required+400` margin; daily 40C handled separately                              | `src/control.ts:257`, `policy.ts:28`                   |
| 2026-08-21 | power/fallback     | `power` `undefined` on WiFi down treated as `0` → never heats                                                                   | `PowerService.ts` `forecastNowKw*1000-HOUSE_BASE_W` (300) or `sun>15°→400W`, `flux.getPower` try/catch              | `src/power.ts:18`, `index.ts:35`                       |
| 2026-08-21 | forecast/generic   | Hard-coded Solcast JSON `PT30M` – broke when provider changed to hourly 1h                                                      | Pluggable `ForecastProvider` interface (`types.ts:14`), `ForecastProcessor.calcKWh` works on any `period_start/end` | `src/forecast/types.ts:14`                             |
